# 1. Use an official Python base image
FROM python:3.11-slim

# 2. Set the working directory inside the container
WORKDIR /app

# 3. Copy the current folder's files into the container
COPY . .

# 4. Run the Python script
CMD ["python", "app.py"]
